from setuptools import setup

setup(
    name='VeroWebTesting',
    version='0.0.1',
    install_requires=[
        'pytest',
        'selenium',
        'webdriver-manager',
        'pyperclip'
    ],
)